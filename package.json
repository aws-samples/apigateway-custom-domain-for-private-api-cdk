{
  "name": "private-domain-api",
  "version": "0.1.0",
  "bin": {
    "app": "bin/app.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk"
  },
  "devDependencies": {
    "aws-cdk": "2.64.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.87.0",
    "constructs": "^10.1.274"
  }
}
